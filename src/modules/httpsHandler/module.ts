export let load = "before";

//before - before webserver running, used to override way which webserver starts
//default - normal plugin loading after server initialisation, have access to creating endpoints